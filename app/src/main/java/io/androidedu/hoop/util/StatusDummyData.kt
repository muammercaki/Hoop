package io.androidedu.hoop.util

/*
import io.androidedu.hoop.model.StatusModel

object StatusDummyData {

    fun getStatusDummyData(): ArrayList<StatusModel> = ArrayList<StatusModel>().apply {
        val statusModel = StatusModel(
            R.drawable.ic_status,
            "Muammer Çakı",
            "Durum güncellemesi eklemek için dokunun"
        )
        add(statusModel)
        add(statusModel)
        add(statusModel)
        add(statusModel)
        add(statusModel)
        add(statusModel)
        add(statusModel)
        add(statusModel)
        add(statusModel)
        add(statusModel)
        add(statusModel)
        add(statusModel)
        add(statusModel)
        add(statusModel)
        add(statusModel)
    }

}
        */